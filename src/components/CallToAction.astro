---
import Button from './Button.astro';
import { Icon } from 'astro-icon/components';

interface Props {
    title: string;
    description: string;
    buttonText: string;
    buttonLink: string;
    secondaryButtonText?: string;
    secondaryButtonLink?: string;
    theme?: 'light' | 'dark' | 'gradient' | 'brand';
    icon?: string;
}

const { 
    title, 
    description, 
    buttonText, 
    buttonLink, 
    secondaryButtonText, 
    secondaryButtonLink, 
    theme = 'light',
    icon
} = Astro.props;

const themeStyles = {
    light: 'bg-white',
    dark: 'bg-primary-black text-white',
    gradient: 'bg-primary-blue text-white',
    brand: 'bg-primary-blue text-white'
};
---

<section class="py-8 px-4 relative overflow-hidden call-to-action-section">
    <div class="container mx-auto">
        <div class={`${themeStyles[theme]} rounded-xl shadow-lg mx-auto max-w-4xl overflow-hidden relative`}>
            <!-- Simple dot pattern overlay for texture -->
            <div class="absolute inset-0">
                {(theme !== 'dark') && (
                    <div class="absolute inset-0 bg-[radial-gradient(rgba(255,255,255,0.1)_1px,transparent_1px)] [background-size:16px_16px] opacity-25"></div>
                )}
            </div>
            
            <div class="relative z-10 p-6 md:p-8 text-center">
                <div class="max-w-3xl mx-auto">                    
                    {icon && (
                        <div class={`w-12 h-12 mx-auto mb-4 rounded-full flex items-center justify-center ${theme === 'dark' || theme === 'gradient' || theme === 'brand' ? 'bg-white/20 text-white' : 'bg-primary-blue/10 text-primary-blue'}`}>
                            <Icon name={icon} class="w-6 h-6" />
                        </div>
                    )}
                    
                    <h2 class="text-2xl md:text-3xl font-bold mb-4 text-white drop-shadow-sm">
                        {title}
                    </h2>
                    
                    <p class="text-base md:text-lg mb-6 text-white/95 max-w-2xl mx-auto leading-relaxed">
                        {description}
                    </p>
                    
                    <div class="flex flex-col sm:flex-row justify-center space-y-3 sm:space-y-0 sm:space-x-4">
                        <Button 
                            href={buttonLink} 
                            variant="glass"
                            size="md"
                            class="group transition-all hover:scale-105 hover:shadow-md bg-white text-primary-blue font-bold border-0 px-6 py-2"
                        >
                            <span class="flex items-center">
                                {buttonText}
                                <Icon name="mdi:arrow-right" class="w-5 h-5 ml-2 transition-transform group-hover:translate-x-1" />
                            </span>
                        </Button>
                        
                        {secondaryButtonText && secondaryButtonLink && (
                            <Button 
                                href={secondaryButtonLink} 
                                variant="outline"
                                size="md"
                                class="bg-transparent text-white border border-white/80 hover:border-white hover:bg-white/10 transition-all hover:scale-105 px-6 py-2"
                            >
                                {secondaryButtonText}
                            </Button>
                        )}
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>